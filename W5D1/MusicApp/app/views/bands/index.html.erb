
  <ul class="bands">
    <% @bands.each do |band| %>
    <li class="band_element_li">
      <div class="band_element">
        <%= link_to band.name, band_url(band) %>
      </div>
    </li>
    <% end %>
  </ul>


<%= link_to "Add Band", new_band_url %>
